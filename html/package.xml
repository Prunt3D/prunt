<package>

  <resource name='Prunt.Web_Server_Resources' format='string'>
    <install mode='copy'>
      <fileset dir=".">
        <include name="*.html"/>
        <include name="*.js"/>
        <include name="*.ts"/>
        <include name="*.map"/>
        <!-- *.ts and *.map included to make debugging a bit easier. -->
      </fileset>
    </install>
  </resource>

</package>
